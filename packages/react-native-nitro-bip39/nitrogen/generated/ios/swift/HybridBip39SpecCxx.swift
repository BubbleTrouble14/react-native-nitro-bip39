///
/// HybridBip39SpecCxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridBip39Spec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
public class HybridBip39SpecCxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::bip39::bridge::swift`)
   * from `Bip39-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.bip39.bridge.swift

  /**
   * Holds an instance of the `HybridBip39Spec` Swift protocol.
   */
  private var implementation: HybridBip39Spec

  /**
   * Get the actual `HybridBip39Spec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridBip39Spec() -> HybridBip39Spec {
    return implementation
  }

  /**
   * Create a new `HybridBip39SpecCxx` that wraps the given `HybridBip39Spec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: HybridBip39Spec) {
    self.implementation = implementation
    /* no base class */
  }

  /**
   * Contains a (weak) reference to the C++ HybridObject to cache it.
   */
  public var hybridContext: margelo.nitro.HybridContext {
    @inline(__always)
    get {
      return self.implementation.hybridContext
    }
    @inline(__always)
    set {
      self.implementation.hybridContext = newValue
    }
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return self.implementation.memorySize
  }

  // Properties
  

  // Methods
  @inline(__always)
  public func getDefaultWordlist() -> Int32 {
    do {
      let result = try self.implementation.getDefaultWordlist()
      return result.rawValue
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func setDefaultWordlist(value: Int32) -> Void {
    do {
      try self.implementation.setDefaultWordlist(value: margelo.nitro.bip39.WordLists(rawValue: value)!)
      return 
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func generateMnemonic(wordCount: bridge.std__optional_double_, rng: bridge.std__optional_std__shared_ptr_ArrayBuffer__, wordlist: bridge.std__optional_WordLists_) -> std.string {
    do {
      let result = try self.implementation.generateMnemonic(wordCount: wordCount.value, rng: rng.value, wordlist: wordlist.value)
      return std.string(result)
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func validateMnemonic(mnemonic: std.string, wordlist: bridge.std__optional_WordLists_) -> Bool {
    do {
      let result = try self.implementation.validateMnemonic(mnemonic: String(mnemonic), wordlist: wordlist.value)
      return result
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func mnemonicToSeed(mnemonic: std.string, password: bridge.std__optional_std__string_) -> ArrayBufferHolder {
    do {
      let result = try self.implementation.mnemonicToSeed(mnemonic: String(mnemonic), password: { () -> String? in
        if let actualValue = password.value {
          return String(actualValue)
        } else {
          return nil
        }
      }())
      return result
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func mnemonicToSeedHex(mnemonic: std.string, password: bridge.std__optional_std__string_) -> std.string {
    do {
      let result = try self.implementation.mnemonicToSeedHex(mnemonic: String(mnemonic), password: { () -> String? in
        if let actualValue = password.value {
          return String(actualValue)
        } else {
          return nil
        }
      }())
      return std.string(result)
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func mnemonicToEntropy(mnemonic: std.string, wordlist: bridge.std__optional_WordLists_) -> std.string {
    do {
      let result = try self.implementation.mnemonicToEntropy(mnemonic: String(mnemonic), wordlist: wordlist.value)
      return std.string(result)
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func entropyToMnemonic(entropy: ArrayBufferHolder, wordlist: bridge.std__optional_WordLists_) -> std.string {
    do {
      let result = try self.implementation.entropyToMnemonic(entropy: entropy, wordlist: wordlist.value)
      return std.string(result)
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
}
